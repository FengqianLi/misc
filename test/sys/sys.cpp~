#include <sys/sysinfo.h>

using namespace std;

    int
main( int argc, char **argv )
{
    struct sysinfo info;
    sysinfo( &info  );
    return (size_t)info.totalram * (size_t)info.mem_unit;
    return 0;
}
